{
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/readmeio/oas.git"
  },
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "attw": "turbo run attw --concurrency=1",
    "bench": "vitest bench",
    "build": "turbo run build",
    "changeset": "changeset",
    "clean": "rm -rf packages/*/node_modules",
    "format": "npx oxlint --fix && npx oxfmt --write",
    "lint": "knip && npm run lint:types && npm run lint:js && npx oxfmt --check",
    "lint:js": "npx oxlint",
    "lint:types": "npm run lint:types --if-present --workspaces",
    "prerelease": "npm run build",
    "release": "changeset publish",
    "version-packages": "changeset version",
    "prelint": "npm run build",
    "pretest": "npm run build",
    "test": "vitest --coverage"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.18.5",
    "@changesets/cli": "^2.30.0",
    "@readme/oxlint-config": "^1.2.1",
    "@vitest/coverage-v8": "^5.0.0",
    "knip": "^5.85.0",
    "oxfmt": "^0.48.0",
    "oxlint": "^1.63.0",
    "tsup": "^8.5.1",
    "turbo": "^2.10.12",
    "vitest": "^5.0.0"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "npm@10.9.4"
}
