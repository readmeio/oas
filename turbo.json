{
  "$schema": "https://v2-10-12.turborepo.dev/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "attw": {
      "dependsOn": ["build", "^build"],
      "outputs": []
    }
  }
}
