---
"oas": patch
---

Resolve Path Item `$ref`s when matching URLs to operations and when collecting common parameters so `components.pathItems` and webhook Path Items no longer drop methods or parameters.
