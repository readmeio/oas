---
"@readme/openapi-parser": patch
---

Fix relative `servers` on webhook path items and operations when an OpenAPI document is loaded from an HTTP(S) URL, and preserve a non-default source port when rewriting those URLs.
