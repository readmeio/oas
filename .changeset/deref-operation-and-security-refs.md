---
"oas": patch
"@readme/oas-to-har": patch
---

Resolve operation-level `$ref`s on webhooks and callbacks, and dereference `$ref` security schemes in HAR auth, so Try It no longer drops summaries, responses, or credentials.
