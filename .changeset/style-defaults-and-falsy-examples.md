---
"oas": patch
"@readme/oas-to-har": patch
---

Honor required defaults when a parameter authors `style`, keep falsy Media Type / parameter `example` values, and resolve Path Item `$ref`s for operation summary and description
