openapi: 3.1.0
info:
  version: '1.0'
  title: Invalid API
servers:
  - url: https://{subdomain}.io
    variables:
      subdomain:
        default: petstore
        description: lorem ipsum
paths:
  /anything:
    get:
      responses:
        '200':
          description: OK
