openapi: 3.1.0
info:
  version: 1.0.0
  title: Invalid API

paths:
  /users:
    get:
      parameters:
        - name: filter
          in: query
          schema:
            type: string
          content:
            application/json:
              schema:
                type: string
      responses:
        default:
          description: hello world

