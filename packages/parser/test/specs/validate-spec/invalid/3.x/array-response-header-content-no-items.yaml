openapi: 3.0.0
info:
  version: '1.0.0'
  title: Invalid API

paths:
  /users:
    get:
      responses:
        'default':
          description: hello world
          headers:
            Content-Type:
              schema:
                type: string
            Last-Modified:
              content:
                application/json:
                  schema:
                    type: array
