openapi: 3.0.0
info:
  version: '1.0.0'
  title: Invalid API

paths:
  /users:
    get:
      parameters:
        - name: tags
          in: query
          content:
            application/json:
              schema:
                type: array
      responses:
        default:
          description: hello world

