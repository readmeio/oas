openapi: 3.2.0
info:
  version: '1.0'
  title: Invalid API
paths:
  /events:
    get:
      responses:
        '200':
          description: A stream of events
          content:
            application/jsonl:
              itemSchema: not-a-schema
