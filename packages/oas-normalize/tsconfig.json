{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // DOM is needed because `oas-normalize` uses `fetch`.
    "lib": ["DOM"],
    "paths": {
      "swagger2openapi": [".sink.d.ts"]
    }
  },
  "include": ["./src/**/*"]
}
