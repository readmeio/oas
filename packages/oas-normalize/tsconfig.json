{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // DOM is needed because `oas-normalize` uses `fetch`.
    "lib": ["DOM"],
    "strict": true
  },
  "include": ["./src/**/*"],
  "exclude": ["dist"]
}
